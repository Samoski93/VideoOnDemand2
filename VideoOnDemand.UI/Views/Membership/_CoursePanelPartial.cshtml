@model CourseDTO

<div class="col-sm-4">
    <div class="panel">
        <img class="thumb" src="@Model.CourseImageUrl" alt="Alternate Text" />
        <div class="panel-body">
            <h3 class="text-dark">@Model.CourseTitle</h3>
            <p>@Model.CourseDescription</p>
            <a class="btn btn-primary" href="~/Membership/Course/@Model.CourseId">View Course</a>
        </div>
    </div>
</div>
